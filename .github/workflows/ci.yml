name: Python CI

on:
  push:
    branches:
      - develop 
  pull_request:
    branches:
      - develop

jobs:

  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]


    steps:
      - name : Print text
        run : |
          echo "Hello, world!"
          echo "This is a GitHub Actions workflow running on Windows."
          